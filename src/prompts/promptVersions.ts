/**
 * The current prompt version.
 * If prompts are edited or improved, updating this version string
 * will automatically invalidate the worksheet generation caches in the database.
 */
export const PROMPT_VERSION = 'v1.0.0';
